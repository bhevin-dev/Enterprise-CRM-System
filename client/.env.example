# Vite environment variables (client-side, exposed to browser)
# The API base URL is proxied via vite.config.js to the server by default.
# Override only if your API is hosted on a different origin.
VITE_API_URL=/api
